# ローカル文字起こしCLI 実装方針

## 1. 概要

Dockerコンテナ内でPythonと`faster-whisper`を実行し、ローカルの音声ファイルを日本語で文字起こしするCLIツールを作成する。

Web画面、FastAPI、Next.js、Laravel、データベースなどは使用しない。

基本的な利用方法は次のとおりとする。

1. ローカルの`inputs`ディレクトリへ音声ファイルを配置する
2. `docker compose run`で文字起こしを実行する
3. 生成された文字起こしファイルをローカルの`outputs`ディレクトリへ保存する

通常は使い捨てコンテナとして実行し、デバッグ時のみbashでコンテナ内へ入れる構成にする。

---

## 2. 料金

### 結論

この構成は、手持ちのPC上だけで実行する限り、追加のサービス利用料なしで利用できる。

以下の利用料金は発生しない。

* OpenAI API利用料
* 音声1分ごとの文字起こし料金
* サーバー利用料
* データベース利用料
* クラウドストレージ利用料
* SaaSの月額料金

使用する主なソフトウェアは以下とする。

* Docker Desktop
* Python
* faster-whisper
* Whisperモデル
* Docker Compose

ただし、厳密には以下の負担がある。

* PCの電気代
* CPU負荷
* 処理時間
* モデルやDockerイメージを保存するディスク容量
* 初回モデルダウンロード時の通信量

そのため、正確には「追加のAPI・サービス料金なし」であり、電気代やPCリソースまで含めた完全な無コストではない。

### Docker Desktopについて

個人利用を前提とする。

会社や組織で業務利用する場合は、Docker Desktopの利用条件を別途確認すること。

---

## 3. 対象環境

想定環境は以下とする。

* OS：macOS
* PC：Intel Mac
* Docker Desktop導入済み
* Docker Compose利用可能
* CPU実行
* GPUは使用しない
* Pythonはホストへ直接インストールしなくてもよい

Python、faster-whisperなどの実行環境はDockerコンテナ内へ閉じ込める。

---

## 4. システム構成

```text
ローカルPC
├── inputs
│   └── 音声ファイル
├── outputs
│   └── 文字起こし結果
├── models
│   └── Whisperモデルのキャッシュ
└── Dockerコンテナ
    ├── Python
    ├── faster-whisper
    └── transcribe.py
```

ローカルディレクトリをDockerコンテナへbind mountする。

```text
ホスト側                    コンテナ側

./inputs       →           /data/inputs
./outputs      →           /data/outputs
./models       →           /models
./app          →           /app
```

`inputs`は読み取り専用でマウントする。

`outputs`と`models`は書き込み可能でマウントする。

これにより、コンテナ内で生成された文字起こし結果は、ローカルの`outputs`ディレクトリへ残る。

---

## 5. ディレクトリ構成

```text
local-transcriber/
├── app/
│   ├── transcribe.py
│   └── requirements.txt
├── inputs/
│   └── .gitkeep
├── outputs/
│   └── .gitkeep
├── models/
│   └── .gitkeep
├── Dockerfile
├── compose.yaml
├── README.md
└── .gitignore
```

### `app/`

Pythonアプリケーションを配置する。

```text
app/
├── transcribe.py
└── requirements.txt
```

### `inputs/`

文字起こし対象の音声ファイルを配置する。

例：

```text
inputs/
└── conversation.mp3
```

入力ファイルは処理中も変更、移動、削除しない。

### `outputs/`

生成した文字起こし結果を保存する。

例：

```text
outputs/
└── conversation.txt
```

入力ファイル名を引き継ぎ、拡張子だけを`.txt`へ変更する。

### `models/`

Whisperモデルのキャッシュを保存する。

コンテナを削除・再作成しても、モデルを再ダウンロードしなくて済むようにする。

### `Dockerfile`

Python実行環境と依存ライブラリを定義する。

### `compose.yaml`

以下を定義する。

* Dockerイメージのビルド
* ローカルディレクトリのbind mount
* モデルキャッシュの保存先
* Pythonスクリプトのエントリーポイント
* 作業ディレクトリ

### `README.md`

環境構築、実行方法、注意事項を記載する。

---

## 6. 通常の利用方法

### 初回ビルド

```bash
docker compose build
```

### 音声ファイルの配置

処理対象ファイルを`inputs`へ配置する。

```text
inputs/conversation.mp3
```

### 文字起こし実行

```bash
docker compose run --rm transcriber "conversation.mp3"
```

処理完了後、以下へ保存する。

```text
outputs/conversation.txt
```

`--rm`を付けることで、処理用の一時コンテナは終了後に削除する。

Whisperモデルや出力ファイルはローカル側へ保存されるため、コンテナが削除されても残る。

---

## 7. コンテナ内へ入る方法

通常利用ではコンテナを常駐させない。

デバッグや動作確認のためにbashへ入りたい場合は、エントリーポイントを一時的に上書きする。

```bash
docker compose run --rm --entrypoint bash transcriber
```

コンテナ内では、例えば次のように実行できる。

```bash
python transcribe.py "conversation.mp3"
```

この方法なら、`docker compose up -d`でコンテナを常駐させる必要はない。

### 常駐コンテナを採用しない理由

今回のツールは、必要なときだけ実行するバッチ処理である。

そのため、常時起動するコンテナには次のような利点がほとんどない。

* HTTPリクエストを待ち受けない
* バックグラウンド処理を継続しない
* 定期実行を行わない
* 外部サービスから呼び出されない

基本運用は`docker compose run --rm`とする。

---

## 8. CLI仕様

### 基本コマンド

```bash
docker compose run --rm transcriber <入力ファイル名>
```

例：

```bash
docker compose run --rm transcriber "conversation.mp3"
```

引数には絶対パスやコンテナ内パスではなく、`inputs`内のファイル名だけを指定する。

Python側で以下のパスへ変換する。

```text
入力：
/data/inputs/<指定されたファイル名>

出力：
/data/outputs/<入力ファイルの拡張子を.txtへ変更した名前>
```

---

## 9. 入力仕様

対応する拡張子は以下とする。

* `.mp3`
* `.wav`
* `.m4a`
* `.flac`

拡張子の大文字・小文字は区別しない。

入力ファイルに対して以下の処理は行わない。

* 上書き
* 移動
* 削除
* リネーム
* 音声形式の恒久的な変換

必要なデコードは文字起こし処理中だけ行う。

---

## 10. 出力仕様

### 保存先

```text
/data/outputs
```

ホスト側では以下に対応する。

```text
./outputs
```

### ファイル名

入力ファイル名を引き継ぎ、拡張子だけを`.txt`へ変更する。

```text
conversation.mp3
↓
conversation.txt
```

### 文字コード

```text
UTF-8
```

### 出力内容

セグメント単位のタイムスタンプ付きTXTとする。

```text
[00:00:00 - 00:00:04]
今日はありがとうございました。

[00:00:04 - 00:00:08]
こちらこそありがとうございました。
```

時間は以下の形式とする。

```text
HH:MM:SS
```

1時間未満の音声でも、形式を統一するため`HH`を含める。

### 同名ファイル

同名の出力ファイルが存在する場合は上書きする。

ただし、上書きしたことを標準出力へ表示する。

---

## 11. 音声認識設定

初期設定は以下とする。

```text
ライブラリ：faster-whisper
モデル：small
言語：ja
デバイス：cpu
計算形式：int8
VAD：有効
単語単位タイムスタンプ：無効
セグメント単位タイムスタンプ：有効
話者分離：無効
```

Python上の概念的な設定は以下とする。

```python
WhisperModel(
    "small",
    device="cpu",
    compute_type="int8",
)
```

文字起こし時は以下を指定する。

```python
model.transcribe(
    input_path,
    language="ja",
    vad_filter=True,
)
```

### モデル選択

初期版は`small`をデフォルトとする。

精度が不足する場合に備え、任意でモデルを指定できるオプションを用意してもよい。

```bash
docker compose run --rm transcriber \
  "conversation.mp3" \
  --model medium
```

対応候補：

* `small`
* `medium`

Intel Macでの利用を前提とするため、初期版では`large-v3`を標準にしない。

---

## 12. 初期プロンプト

固有名詞の認識補助として、任意の初期プロンプトを指定できるようにする。

例：

```bash
docker compose run --rm transcriber \
  "conversation.mp3" \
  --prompt "これは話者Aと話者Bの日本語会話です。"
```

初期プロンプトは認識補助であり、話者を自動的に識別する機能ではない。

---

## 13. 話者分離

初期版では実装しない。

Whisperによる文字起こしだけでは、発言者が話者Aなのか、話者Bなのかを正確に判定できない。

初期版の出力は以下のようになる。

```text
[00:00:00 - 00:00:04]
今日はありがとうございました。

[00:00:04 - 00:00:08]
こちらこそありがとうございました。
```

将来的には別機能として、話者ダイアリゼーションを追加する。

想定構成：

```text
faster-whisper
└── 発言内容と発言時間を取得

話者分離モデル
└── SPEAKER_00、SPEAKER_01と時間を取得

結合処理
└── 文字起こしセグメントへ話者を割り当てる
```

名前はユーザーが指定する。

```text
SPEAKER_00 = 話者A
SPEAKER_01 = 話者B
```

---

## 14. モデルキャッシュ

初回実行時にWhisperモデルをダウンロードする。

モデルキャッシュは以下へ保存する。

```text
コンテナ側：/models
ホスト側：./models
```

環境変数として以下を設定する。

```text
HF_HOME=/models
```

必要に応じて関連キャッシュディレクトリも`/models`配下へ統一する。

これにより、コンテナを削除してもモデルはローカルへ残る。

2回目以降は、保存済みモデルを使用する。

`models`ディレクトリはGit管理対象外とする。

---

## 15. Dockerfile方針

ベースイメージは軽量なPythonイメージを使用する。

```dockerfile
FROM python:3.11-slim
```

作業ディレクトリ：

```dockerfile
WORKDIR /app
```

依存関係のインストール：

```dockerfile
COPY app/requirements.txt /app/requirements.txt

RUN pip install --no-cache-dir \
    -r /app/requirements.txt
```

アプリケーションのコピー：

```dockerfile
COPY app/ /app/
```

エントリーポイント：

```dockerfile
ENTRYPOINT ["python", "/app/transcribe.py"]
```

必要に応じてFFmpegをインストールする。

```dockerfile
RUN apt-get update \
    && apt-get install -y --no-install-recommends ffmpeg \
    && rm -rf /var/lib/apt/lists/*
```

初期版では音声形式の互換性を確保するため、FFmpegを含めてもよい。

---

## 16. Compose方針

`compose.yaml`では以下を設定する。

```yaml
services:
  transcriber:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - type: bind
        source: ./inputs
        target: /data/inputs
        read_only: true
      - type: bind
        source: ./outputs
        target: /data/outputs
      - type: bind
        source: ./models
        target: /models
    environment:
      HF_HOME: /models
```

開発中にホスト側のPythonコードを即時反映したい場合は、以下も追加する。

```yaml
      - type: bind
        source: ./app
        target: /app
```

ただし、完成後は`app`をDockerイメージへ`COPY`し、コードのbind mountを外してもよい。

---

## 17. requirements.txt

初期版では最低限、以下を指定する。

```text
faster-whisper
```

実際に動作確認した後で、再現性を確保するためバージョンを固定する。

例：

```text
faster-whisper==<動作確認済みバージョン>
```

標準ライブラリとして以下を使用する。

* argparse
* pathlib
* sys
* logging

標準ライブラリは`requirements.txt`へ記載しない。

---

## 18. エラー処理

以下を検証する。

### 引数不正

* ファイル名が指定されていない
* 不要な引数が渡された

### 入力ファイル

* 指定ファイルが存在しない
* ディレクトリが指定された
* 対応外の拡張子
* 音声ファイルを読み取れない
* 空のファイル

### 出力

* `outputs`へ書き込めない
* 出力ディレクトリが存在しない
* ファイル保存に失敗した

### モデル

* モデルのダウンロードに失敗した
* モデルの読み込みに失敗した
* キャッシュディレクトリへ書き込めない

### 文字起こし

* 文字起こし中に例外が発生した
* 認識結果が空だった

失敗時は以下の挙動とする。

```text
標準エラー出力へ原因を表示
終了コード1で終了
不完全な出力ファイルは残さない
```

成功時は以下とする。

```text
終了コード0
保存先を標準出力へ表示
```

---

## 19. ログ表示

処理中に最低限、以下を表示する。

```text
入力ファイルを確認しています...
モデルを読み込んでいます...
文字起こしを開始します...
文字起こしが完了しました。
保存しました: /data/outputs/conversation.txt
```

エラー時は原因が分かるメッセージを表示する。

50分程度の音声では処理時間が長くなる可能性があるため、処理開始後に無表示のままにしない。

可能であれば、セグメントが取得されるごとにタイムスタンプや処理位置を表示する。

厳密なパーセント進捗は初期版では必須としない。

---

## 20. `.gitignore`

以下をGit管理対象外とする。

```gitignore
inputs/*
!inputs/.gitkeep

outputs/*
!outputs/.gitkeep

models/*
!models/.gitkeep

__pycache__/
*.pyc

.DS_Store
```

音声ファイル、文字起こし結果、WhisperモデルをGitへコミットしない。

---

## 21. READMEに記載する内容

READMEには最低限、以下を記載する。

### 概要

* ローカル音声を文字起こしするCLIツールであること
* 外部APIへ音声を送信しないこと
* 基本的に追加料金なしで使用できること

### 必要環境

* macOS
* Docker Desktop
* Docker Compose
* インターネット接続は初回モデル取得時のみ必要

### 初回準備

```bash
docker compose build
```

### 実行方法

```bash
docker compose run --rm transcriber "音声ファイル名.mp3"
```

### bashへ入る方法

```bash
docker compose run --rm --entrypoint bash transcriber
```

### 入出力場所

```text
入力：inputs/
出力：outputs/
モデル：models/
```

### 注意事項

* 初回実行時はモデルダウンロードが発生する
* CPU実行なので長時間音声は処理に時間がかかる
* 文字起こし結果は完全な精度を保証しない
* 初期版では話者分離を行わない
* 入力音声は削除・変更しない

---

## 22. 実装順序

### Step 1：Docker環境

以下を作成する。

* Dockerfile
* compose.yaml
* requirements.txt
* ディレクトリ構成

Dockerコンテナ内でPythonを実行できることを確認する。

### Step 2：CLI引数処理

`argparse`で音声ファイル名を受け取る。

以下を検証する。

* 入力ファイルの存在
* 拡張子
* ファイルであること

### Step 3：文字起こし

`faster-whisper`で日本語の文字起こしを行う。

初期設定：

```text
model=small
language=ja
device=cpu
compute_type=int8
vad_filter=true
```

### Step 4：TXT出力

セグメントをタイムスタンプ付きのTXTへ変換する。

`outputs`へUTF-8で保存する。

### Step 5：ログ・エラー処理

成功・失敗メッセージと終了コードを実装する。

### Step 6：README

初回構築から実行までの手順を記載する。

### Step 7：任意オプション

必要に応じて以下を追加する。

* `--model`
* `--prompt`
* `--overwrite`
* `--output-format`
* JSONやSRT出力

---

## 23. 初期版の対象外

以下は初期版では実装しない。

* FastAPI
* Next.js
* Laravel
* ブラウザ画面
* REST API
* ユーザー認証
* データベース
* 履歴管理
* クラウド保存
* 外部文字起こしAPI
* リアルタイム文字起こし
* 複数ファイルの並列処理
* 話者分離
* 男女判定
* 話者A／話者Bの自動割り当て
* 高度なノイズ除去
* 音声編集

---

## 24. 完成条件

以下を満たした時点で初期版完成とする。

* `docker compose build`が成功する
* `inputs`のMP3またはWAVを読み込める
* `docker compose run --rm`で実行できる
* 日本語の文字起こしが行われる
* タイムスタンプ付きTXTが生成される
* TXTがローカルの`outputs`へ保存される
* 入力ファイル名が出力ファイル名へ引き継がれる
* 入力音声が変更・削除されない
* コンテナ削除後も出力とモデルが残る
* bashへ入ってデバッグできる
* エラー時に原因と終了コードが返る
* READMEだけで環境構築と実行ができる

---

## 25. 最終的な利用イメージ

```bash
cd local-transcriber
```

音声ファイルを配置する。

```text
inputs/conversation.mp3
```

文字起こしを実行する。

```bash
docker compose run --rm transcriber "conversation.mp3"
```

処理完了後、以下が生成される。

```text
outputs/conversation.txt
```

デバッグ時はbashへ入る。

```bash
docker compose run --rm --entrypoint bash transcriber
```

通常はコンテナを常駐させず、必要なときだけ使い捨てコンテナを起動する。
